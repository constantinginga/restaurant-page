/* TO-DO:
    Add buttons to Menu Section; Add styling to Menu Section;
    Add 'about' module; change active btn to 'Home'; add tab switching functionality; */

// import generateHomepage from './home.js';
// generateHomepage(content);

const content = document.querySelector('#content');

import generateMenu from './menu.js';
generateMenu(content);